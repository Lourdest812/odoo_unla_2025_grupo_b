<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <!-- LISTA de Tipos de Propiedad (editable en línea) -->
    <record id="view_estate_property_type_list" model="ir.ui.view">
        <field name="name">estate.property.type.list</field>
        <field name="model">estate.property.type</field>
        <field name="arch" type="xml">
            <list string="Tipos de Propiedad" editable="top">
                <field name="name"/>
            </list>
        </field>
    </record>

    <!-- ACCIÓN: sólo list -->
    <record id="estate_property_type_action" model="ir.actions.act_window">
        <field name="name">Tipos de propiedad</field>
        <field name="res_model">estate.property.type</field>
        <field name="view_mode">list</field>
        <field name="views" eval="[(ref('view_estate_property_type_list'), 'list')]"/>
    </record>

</odoo>
